apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
spec:
  selector:
    app: nginx
  ports:
  - port: 80           # Port inside the cluster
    targetPort: 80     # Port on the Pod
    nodePort: 30081    # Port on the Node (must be 30000-32767)
  type: NodePort
